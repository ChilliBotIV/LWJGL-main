package com.example;

public class Bezier {
    float lineCreation(float x1, float x2) {
        // float pointCount = (float)(lineCreation(x1, x2));
        // for (lineCreation(x1, x2))

        // Vector3f point = weightedAverage(lineCreation(x1, x2));
        // plot(point);

        return 0;
    }

    float linearinterpolation(float x1, float x2, float p) {
        return 0;
    }
}
